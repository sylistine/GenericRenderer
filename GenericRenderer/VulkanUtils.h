#pragma once

#include "stdinc.h"

#include <vulkan/vulkan.hpp>

namespace VkUtil
{
    // This and DaedalusVkClasses, it turns out, were basically dupes
    // of everything in vulkan.hpp.
    // I will probably find a use for this in the future, so it stays.
}
